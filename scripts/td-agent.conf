<source>
  @type http
  port 8888
  bind 0.0.0.0
  body_size_limit 32m
  keepalive_timeout 10s
</source>

<source>
  type forward
</source>

<match **>
	type copy
	include_tag_key true
	<store>
		type elasticsearch
		logstash_format true
		flush_interval 1s
	</store>
</match>

